# Use a separate, empty Supabase project created only for restoration tests.
# The restore script refuses to use the source/production project and refuses
# to write into a target containing any rows in the protected tables.
RESTORE_TARGET_SUPABASE_URL=
RESTORE_TARGET_SERVICE_ROLE_KEY=
RESTORE_CONFIRM=RESTORE_TO_EMPTY_NON_PRODUCTION

